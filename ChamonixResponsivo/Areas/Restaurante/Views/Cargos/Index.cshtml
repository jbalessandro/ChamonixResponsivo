@model IEnumerable<Chamonix.Domain.Models.Admin.Cargo>

@{
    ViewBag.Title = "Cargos";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ativo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ativo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CargoId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.CargoId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.CargoId })
        </td>
    </tr>
}

</table>
